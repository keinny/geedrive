info:
  name: Drivers analytics
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:8000/drivers/analytics
  headers:
    - name: Authorization
      value: Bearer {{FLEET_API_KEY}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
