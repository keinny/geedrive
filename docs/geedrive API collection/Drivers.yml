info:
  name: Drivers
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/drivers
  headers:
    - name: Authorization
      value: Bearer {{FLEET_API_KEY}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
